# ─── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python
build/
dist/
*.egg-info/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Type checking
.mypy_cache/
.dmypy.json
dmypy.json

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.coveragerc

# Ruff
.ruff_cache/

# ─── Terraform ───────────────────────────────────────────────────────────────
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plans — never commit plans; generate fresh per run
*.tfplan

# ─── Secrets — NEVER commit any of these ─────────────────────────────────────
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
*secret*
*credential*
*_secrets.json
*_credentials.json

# The two glob patterns above are for credential/secret *data*. Until 2026-07-29 they also
# matched *source*, and silently excluded from the repository: 11 Python modules — including
# `credential_client.py` (the shared base class), `connection_credential_resolver.py`, and the
# `credential_expiry_notifier_handler` Lambda — plus the whole `infrastructure/modules/secrets`
# Terraform module that all three environments reference. A fresh clone could not import and
# `terraform validate` could not run, so CI had failed on every run for months while every check
# passed on developer machines, where the files exist untracked on disk.
#
# Order matters: a file cannot be re-included while its parent directory is excluded, so the
# directory negations must come first. Data files (.json/.env/.pem/.secrets.baseline) still match
# the patterns above and stay ignored.
!*credential*/
!*secret*/
!*credential*.py
!*secret*.py
!*credential*.tf
!*secret*.tf
.env
.env.*
!.env.example

# ─── AWS ─────────────────────────────────────────────────────────────────────
.aws/

# ─── IDE ─────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ─── Claude Code — personal/local only; settings.json, commands/, hooks/ ARE committed ────
.claude/settings.local.json
.claude/scheduled_tasks.lock

# ─── Logs ────────────────────────────────────────────────────────────────────
*.log
logs/

# ─── Build outputs ───────────────────────────────────────────────────────────
target/
out/
