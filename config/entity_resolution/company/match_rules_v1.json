{
  "_comment": "Match rules for dim_company. Merges Salesforce Account + NetSuite Customer into one canonical company record.",
  "entity_type": "company",
  "rule_set_version": "v1",

  "blocking": {
    "_comment": "Pre-partition by first 3 chars of normalised company name. Works regardless of which sources are present (Salesforce, NetSuite, or both). Email-domain blocking was removed because Salesforce Account does not carry an email field; name-prefix blocking is source-agnostic.",
    "key_type": "name_first3",
    "source_field": "full_name",
    "max_block_size": 500
  },

  "rules": [
    {
      "_comment": "Rule 1 — Deterministic: identical normalised email address = same company billing contact → same company.",
      "rule_id": "email-exact",
      "strategy": "deterministic",
      "fields": [
        { "field_name": "email_address", "normalise": true }
      ]
    },
    {
      "_comment": "Rule 2 — Probabilistic: company name similarity + same country. Catches name variants like 'Acme Corp' vs 'ACME Corporation'.",
      "rule_id": "name-country-fuzzy",
      "strategy": "probabilistic",
      "match_threshold": 0.85,
      "fields": [
        { "field_name": "full_name",       "weight": 0.70, "similarity_kind": "jaro_winkler" },
        { "field_name": "billing_country", "weight": 0.30, "similarity_kind": "exact" }
      ]
    }
  ]
}
